# The skill pins an exact viem version in package.json, so a lock file adds no
# reproducibility and only creates churn when the skill is copied.
package-lock.json
bun.lock
bun.lockb
yarn.lock
pnpm-lock.yaml
